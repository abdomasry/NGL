{
  "name": "node-graph-editor-deployment",
  "private": true,
  "version": "1.0.0",
  "description": "Root deployment manifest for the Node Graph Editor backend.",
  "workspaces": [
    "NGL/backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=node-graph-backend",
    "start": "npm run build && npm run start --workspace=node-graph-backend"
  },
  "dependencies": {
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
